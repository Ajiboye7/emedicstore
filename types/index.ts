export interface CustomButtonProps{
    title: String
    buttonStyle:string
    iconStyle: string
    Icon:string
    titleStyle: string
}

export interface TextTitleProps{
    textStyle?:string
    children?: React.ReactNode;
}

