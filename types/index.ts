export interface CustomButtonProps{
    title: String
    buttonStyle:string
    iconStyle: string
    Icon:string
}

export interface TextTitleProps{
    textStyle?:string
    children?: React.ReactNode;
    
} 